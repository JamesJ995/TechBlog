<div class='row mb-4 project'>
  <div class='col-md-5'>
    <h2>
      <h2>{{post.title}}</h2>
    </h2>
    <p>Posted by:
      {{user.userName}}
      on
      {{format_date post.date_created}}</p>
  </div>
  <div class='col-md-7'>
    <p>
      {{post.text}}
    </p>
  </div>
</div>